# Shopizer Spring Boot API base (no trailing slash).
# Used only by next.config.ts rewrites: /shopizer-api/* → this URL.
# Copy to .env.local — do not commit .env files.
SHOPIZER_API_URL=http://localhost:8080/api
