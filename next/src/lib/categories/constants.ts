/** Port of `src/app/pages/shared/validation/validators.ts` used by categories. */
export const ALPHANUMERIC_WITH_HYPHEN = /^[a-zA-Z0-9-_]+$/;
export const NUMBER_PATTERN = /^[0-9]+$/;

/** Angular `CategoriesListComponent.perPage`. */
export const CATEGORIES_PER_PAGE = 25;

/** Angular `CategoryFormComponent.perPage` for the parent picker. */
export const CATEGORY_PARENT_COUNT = 100;
