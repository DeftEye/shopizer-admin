# Shopizer Spring Boot API (leave this repo's API unchanged).
# Angular equivalent: src/environments/environment.ts → apiUrl
SHOPIZER_API_URL=http://localhost:8080/api

# Browser calls this same-origin path; Next rewrites it to SHOPIZER_API_URL.
NEXT_PUBLIC_API_BASE=/shopizer-api
